{
  "name": "newbootstrap",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "start": "parcel serve source/index.html --public-url / --dist-dir dist",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "parcel build --public-url ./ ./source/index.html --dist-dir build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@parcel/transformer-sass": "^2.8.3",
    "parcel": "^2.8.3"
  },
  "dependencies": {
    "@popperjs/core": "^2.11.7",
    "bootstrap": "^5.2.3"
  }
}
